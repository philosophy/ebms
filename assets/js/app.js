com = {
    ebms: {
        widgets: {},
        util: {},
        views: {},
        init: function() {
           this.widgets.header.init();
           this.widgets.confirm.init();
           this.widgets.search.init();
           this.widgets.ajax.init();
        }
    }
};